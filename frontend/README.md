# Frontend Placeholder

This folder is reserved for the React + Vite frontend application.

No feature code is added yet. UI implementation must follow `docs/13-brand-system.md` and `docs/design/tokens-v1.json`.
